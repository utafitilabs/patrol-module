{# Widget "chstation" — patrols by station, ranked (PL·10).

   THE CHART IS THE ATLAS'S. What this module states is the ranking — the five
   busiest stations this month and their counts
   (Uhifadhi\Patrol\Model\PatrolDashboard::stationChart); what a comparison
   across categories looks like is the component's to decide, and is the same
   decision on every chart in the product. #}
<div class="c" data-patrol-stations>
    <span class="tab">Patrols by station<span class="src">· this month</span></span>
    {{ atlas_chart(dashboard.stationChart) }}
</div>
